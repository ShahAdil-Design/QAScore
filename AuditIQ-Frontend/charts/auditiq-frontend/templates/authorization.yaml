apiVersion: "security.istio.io/v1"
kind: AuthorizationPolicy
metadata:
  name: {{ include "helpers.name" . }}-authz-allow-anonymous
  namespace: {{ include "helpers.namespace" . }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "helpers.name" . }}
  rules:
    - to:
      - operation:
          paths: ["*"]